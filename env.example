# Application Environment
NODE_ENV=development
BASE_HTTPS_URL=https://localhost:8080
BASE_API_URL=https://localhost:8080/api
BASE_WSS_URL=wss://localhost:8080/ws/

# Set to 'true' to bypass Two-Factor Authentication during login.
BYPASS_2FA=true

# Database Path (for SQLite, volume mounted)
DB_PATH=/db/database.sqlite

# JWT Secrets
# For local development, we use simple strings.
PREAUTH_TOKEN_SECRET=your-preauth-secret-for-dev
ACCESS_TOKEN_SECRET=your-access-secret-for-dev
REFRESH_TOKEN_SECRET=your-refresh-secret-for-dev
CLI_TOKEN_SECRET=your-cli-secret-for-dev

# Optional: Email OTP (for 2FA)
# Replace dummy values with your own SMTP credentials if you want to test email OTP locally.
SMTP_HOST=smtp.ethereal.email # A common test SMTP host for Nodemailer (not real)
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=test@example.com # Dummy user
SMTP_PASS=password123 # Dummy password
EMAIL_FROM_NAME=NetPong Admin
EMAIL_FROM_ADDRESS=noreply@netpong.example.com

# Optional: SMS OTP (for 2FA)
# Replace dummy values with your own Twilio credentials if you want to test SMS OTP locally.
TWILIO_ACCOUNT_SID=ACxxxdummyyyytokenzzzzzzzzzzz # Dummy SID
TWILIO_AUTH_TOKEN=fakeAuthToken123xyzABC4567890  # Dummy Auth Token
TWILIO_PHONE_NUMBER=+15005550006 # Dummy phone number

# Optional: Google Sign-in
# Replace Dummy values with your own Google OAuth 2.0 credentials to test google sign in locally.
GOOGLE_CLIENT_ID=000000000000-dummyclientid0000000000000000.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-dummy_client_secret_abc123
# GOOGLE_REDIRECT_URI=https://localhost:8080/api/auth/google/callback